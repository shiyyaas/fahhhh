import 'package:flutter/material.dart';

class MySubject extends StatelessWidget {
  const MySubject({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('MySubject Screen')));
  }
}
